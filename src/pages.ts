export const app_path = "/app"

export const sign_in_path = "/sign-in"

export const sign_up_path = "/sign-up"

export const first_steps_path = `${sign_up_path}/first-steps`

export const start_path = "/home";

export const project_base_path = `${app_path}/projects`

export const project_path = `${project_base_path}/:projectId`

export const project_edit_path = `${project_path}/edit`
